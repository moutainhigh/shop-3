<?xml version="1.0" encoding="GBK"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><!-- create by autoCode,v1.0.0 --><mapper namespace="manage.express">	<select id="manage.express.selectPageList" parameterType="com.tuisitang.modules.shop.entity.Express" resultType="com.tuisitang.modules.shop.entity.Express">	 select * 	 from t_express t where 1=1  order by sort   limit #{offset},#{pageSize} 	</select> 	<select id="manage.express.selectPageCount" parameterType="com.tuisitang.modules.shop.entity.Express" resultType="java.lang.Integer">	 select count(*) from (select 1 	 from t_express t where 1=1 	 ) a 	</select> 	<insert id="manage.express.insert" parameterType="com.tuisitang.modules.shop.entity.Express" useGeneratedKeys="true" keyProperty="id">		insert into t_express		(		id,fee		<if test="code!=null and code!=''">		,code		</if>		<if test="name!=null and name!=''">		,name		</if>		<if test="sort!=0">		,sort		</if>		)		values		(		id,#{fee}		<if test="code !=null and code !=''">		,#{code}		</if>		<if test="name !=null and name !=''">		,#{name}		</if>		<if test="sort !=0">		,#{sort}		</if>	)</insert>	<update id="manage.express.update" parameterType="com.tuisitang.modules.shop.entity.Express">		update t_express		  set id=#{id},fee=#{fee}		<if test="code !=null and code !=''">		,code=#{code}		</if>		<if test="name !=null and name !=''">		,name=#{name}		</if>		<if test="sort !=0">		,sort=#{sort}		</if>		 where 1=1 and id=#{id}	</update>	<select id="manage.express.selectOne" parameterType="com.tuisitang.modules.shop.entity.Express" resultType="com.tuisitang.modules.shop.entity.Express">	 select * from t_express t where 1=1 and id=#{id}	</select>	<delete id="manage.express.deleteById" parameterType="com.tuisitang.modules.shop.entity.Express">		delete from t_express where 1=1 and id=#{id}	</delete>		<!-- 6. 根据条件查询Express -->	<select id="manage.express.selectList" parameterType="com.tuisitang.modules.shop.entity.Express" resultType="com.tuisitang.modules.shop.entity.Express">		select t.* from t_express t where 1 = 1 order by sort	</select></mapper>